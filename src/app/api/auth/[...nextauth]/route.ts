//Define las rutas de la API que manejan las solicitudes de autenticación.

import { handlers } from "@/auth" // Referring to the auth.ts we just created - Refiriéndonos al archivo auth.ts que acabamos de crear
export const { GET, POST } = handlers